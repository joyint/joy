# Generated by Joy {{ joy_version }} -- do not edit manually

[agent]
display_name = "{{ agent.name }}"
description = "{{ agent.description }}"
safety = "{% if agent.capability == 'review' %}high{% else %}medium{% endif %}"
agent_type = "{{ agent.capability }}"
enabled_tools = [{% for p in agent.permissions.allowed %}"{{ p }}"{% if not loop.last %}, {% endif %}{% endfor %}]
