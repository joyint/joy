id: JOY-00D2-C1
title: Publish prebuilt binaries via cargo-dist
type: story
status: closed
priority: medium
assignees:
- member: ai:claude@joy
capabilities:
- plan
- implement
- review
effort: 3
created_by: ai:claude@joy
created: 2026-04-13T17:01:04.777282937Z
updated: 2026-04-13T17:34:19.098897834Z
description: |-
  ## Goal

  Ship prebuilt Joy binaries on GitHub releases so users can install via a
  one-line shell command instead of going through ``cargo install``.

  ## Approach

  Use [cargo-dist](https://axodotdev.github.io/cargo-dist/) (the Rust
  community's standard for this, also used by ruff, uv, oxc). It generates
  a GitHub Actions release workflow and shell/PowerShell installer scripts
  that live next to the release assets on GitHub.

  ### Scope

  1. Add cargo-dist config to ``Cargo.toml`` (workspace metadata).
  2. Generate the release workflow with ``cargo dist init`` and adjust for
     repo conventions (PR-based release via ``just release`` recipe, signed
     tags etc.).
  3. Build targets: Linux x86_64 + aarch64, macOS x86_64 + aarch64, Windows
     x86_64.
  4. Installer artifacts produced per release:
     - ``joy-installer.sh`` (bash)
     - ``joy-installer.ps1`` (PowerShell)
     - Raw tarballs / zips per target triple
  5. Verify via a prerelease tag that the workflow produces all artifacts
     and that ``curl -fsSL
     github.com/joyint/joy/releases/latest/download/joy-installer.sh | sh``
     actually installs the correct binary.
  6. Document the new install command in Joy's README and CONTRIBUTING.

  ### Out of scope

  - ``get.joyint.com`` vanity domain (tracked in the website backlog).
  - Installing docs update on joyint.com (tracked in the website backlog).

  ## Acceptance

  - Tagging a release triggers the workflow.
  - ``joy-installer.sh`` on the release page installs Joy into
    ``~/.local/bin`` (or equivalent) on Linux / macOS.
  - README mentions the new one-liner as primary install method,
    ``cargo install joy-cli`` retained as alternative.
comments:
- author: ai:claude@joy
  date: 2026-04-13T17:34:19.041364257Z
  text: |-
    [x] Installed cargo-dist 0.31.0 locally for workflow generation.
    [x] Ran 'dist init --hosting github'; created dist-workspace.toml with targets aarch64/x86_64 for Linux/macOS and x86_64 for Windows.
    [x] Enabled shell + powershell installers in dist-workspace.toml; install-path set to '~/.local/bin' (XDG standard, typically in PATH).
    [x] Added [profile.dist] to Cargo.toml (thin LTO, inherits release).
    [x] Generated .github/workflows/release.yml via 'dist generate'. Triggers on version tag push, PR dry-run included.
    [x] 'dist plan' verified artifacts: joy-cli-installer.sh, joy-cli-installer.ps1, platform tarballs/zips with joy binary + LICENSE + README.md.
    [x] Updated README.md Quick Start: curl | sh installer as primary, 'cargo install joy-cli' as alternative.
    [x] just check green. Note that 'joy' on crates.io is taken (joystick lib) so the installer artifact stays named 'joy-cli-installer.sh'; the vanity URL 'get.joyint.com/joy' handled separately (JISITE-001B-37) keeps the end-user experience clean.

    Remaining: first real tag push will exercise the workflow end-to-end; any config issues surface there and are iterated.
