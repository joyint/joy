name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  # For the host key pins alone (job host-key-pins): the three public
  # forges rotate keys, and a pin that nothing checks again is a pin
  # that is right until the day it is not.
  schedule:
    - cron: "17 4 * * *"
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-Dwarnings"

# Read-only access is enough: this workflow does not push, comment,
# or create releases. Tightens the default GITHUB_TOKEN scope.
permissions:
  contents: read

# joy-core depends on joy-crypt via a path dependency
# (`../crypt/crates/joy-crypt`) plus a version constraint so that
# `cargo publish` resolves the version from crates.io while local
# builds use the sibling checkout. The standalone joy CI therefore
# also clones joyint/crypt into a sibling directory before running
# any cargo command. On GitHub both repos are public and no PAT is
# needed; on a private mirror (e.g. the internal Gitea) the sibling
# is private too, so the checkout uses the ORG_READ_TOKEN org secret
# when present and falls back to the default token otherwise.

jobs:
  fmt:
    name: Format
    runs-on: ubuntu-latest
    # The nightly schedule is for the pins and nothing else. Every
    # other job needs this one, and a skipped job skips what needs it,
    # so this single condition keeps the build and test matrix off the
    # nightly run.
    if: github.event_name != 'schedule'
    steps:
      - uses: actions/checkout@v4
        with:
          path: joy
      - uses: actions/checkout@v4
        with:
          repository: joyint/crypt
          ref: main
          path: crypt
          token: ${{ secrets.ORG_READ_TOKEN || github.token }}
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all -- --check
        working-directory: joy

  lint:
    name: Lint
    runs-on: ubuntu-latest
    needs: fmt
    steps:
      - uses: actions/checkout@v4
        with:
          path: joy
      - uses: actions/checkout@v4
        with:
          repository: joyint/crypt
          ref: main
          path: crypt
          token: ${{ secrets.ORG_READ_TOKEN || github.token }}
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: joy
          # Persist the dependency/target cache even when the job fails, so
          # iterating on a red build does not recompile every dependency from
          # scratch on the next run (matters most for the slow windows leg).
          cache-on-failure: true
      - run: cargo clippy --workspace --all-targets --all-features -- -D warnings
        working-directory: joy

  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    needs: fmt
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
    steps:
      - uses: actions/checkout@v4
        with:
          path: joy
      - uses: actions/checkout@v4
        with:
          repository: joyint/crypt
          ref: main
          path: crypt
          token: ${{ secrets.ORG_READ_TOKEN || github.token }}
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: joy
          cache-on-failure: true
      # joy-core's identity resolution reads git user.email as the local
      # fallback identity; GitHub-hosted runners have none configured, so set
      # one (dev machines and the self-hosted runner image already have it).
      - name: Configure a git identity for the tests
        run: |
          git config --global user.email "ci@joyint.dev"
          git config --global user.name "Joyint CI"
      # --no-fail-fast: run every test binary even if an earlier one fails,
      # so a single CI run surfaces all platform-specific failures at once
      # (important for the slow windows-latest leg). See JOY-01A4-5B.
      - run: cargo test --workspace --no-fail-fast
        working-directory: joy

  # Split out of the matrix so the windows leg can differ from linux when a
  # venue needs it. It runs everywhere: GitHub uses hosted runners, the
  # internal Gitea mirror serves windows-latest from the Windows VM on zara
  # (infra: win-provision-gitea).
  test-windows:
    name: Test (windows-latest)
    runs-on: windows-latest
    needs: fmt
    steps:
      - uses: actions/checkout@v4
        with:
          path: joy
      - uses: actions/checkout@v4
        with:
          repository: joyint/crypt
          ref: main
          path: crypt
          token: ${{ secrets.ORG_READ_TOKEN || github.token }}
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: joy
          cache-on-failure: true
      - name: Configure a git identity for the tests
        run: |
          git config --global user.email "ci@joyint.dev"
          git config --global user.name "Joyint CI"
      - run: cargo test --workspace --no-fail-fast
        working-directory: joy

  # The Codeberg pin build step of design D1.4a, run again: every
  # recorded blob is checked against the key the host really serves and
  # against the fingerprint its forge publishes (`just
  # check-host-key-pins`). It runs nightly and on demand, never on a
  # pull request, because it talks to three forges over the network and
  # their outage is nobody's merge to block. A red run means a forge
  # rotated a key or a pin was edited by hand; both need a person.
  host-key-pins:
    name: Host key pins
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4
        with:
          path: joy
      - uses: extractions/setup-just@v3
      # jq, curl, ssh-keygen and ssh-keyscan are on the hosted image;
      # no cargo runs here, so the crypt sibling is not needed either.
      - run: just check-host-key-pins
        working-directory: joy

  integration:
    name: Integration (bats)
    runs-on: ubuntu-latest
    needs: fmt
    steps:
      - uses: actions/checkout@v4
        with:
          path: joy
      - uses: actions/checkout@v4
        with:
          repository: joyint/crypt
          ref: main
          path: crypt
          token: ${{ secrets.ORG_READ_TOKEN || github.token }}
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: joy
          cache-on-failure: true
      - name: Install bats
        run: sudo apt-get update && sudo apt-get install -y bats
      # The bats suite drives the debug joy binary built with fast-kdf
      # (minimal Argon2id) for speed, matching `just test-int`. The
      # setup helper finds it at target/debug/joy. Windows coverage is
      # tracked separately (JOY-01A7-35).
      # the forge plugins ride the bats PATH too (forge_alias.bats,
      # JOY-0251-AA); fast-kdf stays a joy-cli-only feature
      - run: cargo build -p joy-cli -p joy-github -p joy-gitlab -p joy-gitea --features joy-cli/fast-kdf
        working-directory: joy
      - run: bats tests/integration/*.bats
        working-directory: joy
