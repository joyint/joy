{
  "name": "joyint-app",
  "version": "0.5.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "echo 'no tests yet'",
    "lint": "echo 'no linter configured yet'",
    "format": "echo 'no formatter configured yet'"
  },
  "devDependencies": {
    "eslint": "^10.0.3",
    "prettier": "^3.8.1"
  }
}
